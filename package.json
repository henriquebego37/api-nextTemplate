{
  "private": true,
  "scripts": {
    "dev": "npx yarn && npx prisma db pull && npx prisma generate && next",
    "build": "npx prisma db pull && npx prisma generate && next build",
    "start": "next start"
  },
  "dependencies": {
    "@prisma/client": "^5.5.2",
    "next": "latest",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "swr": "^2.0.0",
    "bcrypt": "^5.1.1"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/react": "^18.0.14",
    "@types/react-dom": "^18.0.5",
    "prisma": "^5.5.2",
    "typescript": "^4.7.4"
  }
}
